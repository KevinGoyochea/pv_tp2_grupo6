const calcularAreaCirculo = (radio) => {
    const area = Math.PI * Math.pow(radio, 2);
    console.log("El area del circulo con radio = " + radio + " es: " + area);
  };
  
  calcularAreaCirculo(7.5);